{
  "manifest_version": "0.3",
  "name": "mcp-connectwise-psa",
  "display_name": "ConnectWise PSA",
  "version": "0.10.0",
  "description": "ConnectWise PSA (Manage) — tickets, time entries, companies, and configurations",
  "long_description": "MCP server for the ConnectWise PSA (Manage) REST API. Search and update tickets, log and review time entries, and look up companies and configurations — using your own ConnectWise API member keys, so everything is attributed to you.",
  "author": {
    "name": "selic",
    "url": "https://github.com/selic"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mspstack/mcp-connectwise-psa"
  },
  "homepage": "https://github.com/mspstack/mcp-connectwise-psa",
  "keywords": [
    "connectwise",
    "connectwise-manage",
    "psa",
    "msp",
    "tickets",
    "time-entries"
  ],
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/index.js"
      ],
      "env": {
        "CW_SITE": "${user_config.cw_site}",
        "CW_COMPANY_ID": "${user_config.cw_company_id}",
        "CW_CLIENT_ID": "${user_config.cw_client_id}",
        "CW_PUBLIC_KEY": "${user_config.cw_public_key}",
        "CW_PRIVATE_KEY": "${user_config.cw_private_key}",
        "CW_MEMBER_IDENTIFIER": "${user_config.cw_member_identifier}",
        "CW_TOOLSETS": "${user_config.cw_toolsets}",
        "CW_DB_HOST": "${user_config.cw_db_host}",
        "CW_DB_NAME": "${user_config.cw_db_name}",
        "CW_DB_USER": "${user_config.cw_db_user}",
        "CW_DB_PASSWORD": "${user_config.cw_db_password}",
        "CW_DB_QUERY_LIBRARY": "${user_config.cw_db_query_library}"
      }
    }
  },
  "user_config": {
    "cw_site": {
      "type": "string",
      "title": "ConnectWise site",
      "description": "Your ConnectWise host, e.g. na.myconnectwise.net or an on-prem hostname",
      "required": true
    },
    "cw_company_id": {
      "type": "string",
      "title": "Company ID",
      "description": "The company id you log in to ConnectWise with",
      "required": true
    },
    "cw_client_id": {
      "type": "string",
      "title": "Integration client ID",
      "description": "Register an integration at developer.connectwise.com to get one",
      "required": true
    },
    "cw_public_key": {
      "type": "string",
      "title": "API public key",
      "description": "ConnectWise → My Account → API Keys",
      "required": true,
      "sensitive": true
    },
    "cw_private_key": {
      "type": "string",
      "title": "API private key",
      "description": "Shown once when the key pair is created",
      "required": true,
      "sensitive": true
    },
    "cw_member_identifier": {
      "type": "string",
      "title": "Member identifier (optional)",
      "description": "Your ConnectWise member id — enables \"my tickets\" and \"my time\" tools on older on-prem versions",
      "required": false
    },
    "cw_toolsets": {
      "type": "string",
      "title": "Toolsets (optional)",
      "description": "Comma list of toolsets or presets (default: all). Add \"sql\" to enable read-only database queries. An unknown name stops the server from starting.",
      "required": false
    },
    "cw_db_host": {
      "type": "string",
      "title": "Database host (optional, on-prem only)",
      "description": "ConnectWise SQL Server host, or host\\INSTANCE. Enables the sql toolset; requires the database, user and password below.",
      "required": false
    },
    "cw_db_name": {
      "type": "string",
      "title": "Database name (optional)",
      "description": "ConnectWise Manage database, e.g. cwwebapp_acme",
      "required": false
    },
    "cw_db_user": {
      "type": "string",
      "title": "Database login (optional)",
      "description": "Dedicated read-only login (db_datareader) — never the ConnectWise application login",
      "required": false
    },
    "cw_db_password": {
      "type": "string",
      "title": "Database password (optional)",
      "description": "Password for the read-only database login",
      "required": false,
      "sensitive": true
    },
    "cw_db_query_library": {
      "type": "string",
      "title": "Saved-query file (optional)",
      "description": "Path to a writable JSON file for saved SQL queries. Without it the built-in queries are read-only.",
      "required": false
    }
  },
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=20"
    }
  }
}
